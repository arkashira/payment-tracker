[tool.poetry]
name = "payment-tracker"
version = "0.1.0"
description = "Real-time payment transaction tracking system"
authors = ["Axentx OS <contact@axentx.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]
pytest = "^7.4.0"
